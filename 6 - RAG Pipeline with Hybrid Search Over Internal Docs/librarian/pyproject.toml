[project]
name = "librarian"
version = "0.1.0"
description = "RAG pipeline with hybrid search over internal docs: ingest, hybrid retrieve (dense + BM25 + RRF), rerank, answer with verified citations."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "pydantic-settings>=2.2",
    "beautifulsoup4>=4.12",
    "pymupdf>=1.24",
    "pyyaml>=6.0",
    "openai>=1.30",
    "chromadb>=0.5",
    "rank-bm25>=0.2.2",
]

[dependency-groups]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "mypy>=1.10",
    "types-pyyaml",
    "types-beautifulsoup4",
]
rerank = [
    "sentence-transformers>=3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.mypy]
strict = true
files = ["librarian", "tests"]
explicit_package_bases = true

[[tool.mypy.overrides]]
module = ["pymupdf", "rank_bm25", "sentence_transformers", "chromadb", "chromadb.*"]
ignore_missing_imports = true

[tool.uv]
package = false
